services:
  api:
    build: 
      context: .
      target: dev # Usamos el stage donde está Go instalado
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    #environment:
    #  - DB_URL=postgres://user:pass@db:5432/mydb
    #depends_on:
    #  - db

  #db:
  #  image: postgres:15-alpine
  #  environment:
  #    - POSTGRES_USER=user
  #    - POSTGRES_PASSWORD=pass
  #    - POSTGRES_DB=mydb
  #  ports:
  #    - "5432:5432"

 